# ⛔ MORTO. Não importar, não ler.
#
# A S12 / S13 / S15 / S16 vivem em `gerar_tempo_util.py` (na raiz do repo).
#
# Isto era uma versão ANTIGA do pipeline — com a S12 PARTIDA: agarrava-se ao último
# CRUZAMENTO em vez da última PANCADA. Custou recall 93,2 → 97,0 quando se corrigiu.
#
# ⚠️ E ENGANAVA QUEM CÁ CHEGASSE. O `REGRAS_DO_VASCO.md` mandava aqui quatro regras
#    marcadas ✅ ("estou aqui") — e o que estava aqui não era o que corre.
#
#    Um ponteiro para um ficheiro MORTO dá erro, e alguém corrige.
#    Um ponteiro para um ficheiro REAL QUE NÃO CORRE leva-te a ler a versão errada
#    da regra — e a acreditar nela. É PIOR.
#
# 🔎 Verificado a 13 jul 2026: funções aqui = vai_e_vem · cruzamentos · rallies ·
#    avaliar · _erro_theta · _tracklets. Funções que existiam AQUI e não no pipeline:
#    NENHUMA. Não era um arquivo — era um engodo.
#
# 📜 O código original está no git (commit 087b0cc). `git log` traz-no de volta.
#
# 🛡️ Apanhado pelo `verificar_fonte.py` — [✅ ONDE = ENGODO VIVO].
